import { Routes } from '@angular/router';
import { Hello } from './hello/hello';
import { Child } from './child/child';

export const routes: Routes = [{path:'',component:Hello},{path:'',component:Child}];
